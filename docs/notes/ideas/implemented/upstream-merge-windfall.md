# Implemented: upstream merge brings 12 Rust files free (group C)

- **Status:** done by merging (no manual port)
- **Source:** `docs/notes/upstream-port-backlog.md` group C (~2,700 lines)
- **Summary:** `drift_detector` (+608), `log_compressor` (stub→real),
  `search_compressor`, text/smart crushers, `magika_detector`,
  offload-pipeline plumbing, diff/unidiff/adaptive/detection fixes,
  `relevance/embedding`, `headroom-simulators` crate + e2e tests, parity
  harness extension. Nothing to port — just take the merge.
